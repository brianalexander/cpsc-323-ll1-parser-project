first(Ms)=> { S-> (A->id, D->(int, float, bool, Epsilon), if, while, begin), Epsilon}
first(S)=> { A->id, D->(int, float, bool, Epsilon), if, while, begin }
follow(Ms)=> 
follow(S)

first(S)=> { A->id, D->(int, float, bool), if, while, begin }
first(Ms)=> { S-> (A->id, D->(int, float, bool), if, while, begin), Epsilon}

follow(Ms)=> {end, $}
follow(S) => {S-> (A->id, D->(int, float, bool), if, while, begin), Epsilon}